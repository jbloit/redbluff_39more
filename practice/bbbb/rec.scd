(
Server.default.record(duration: 4, bus: ~o1.index, numChannels:5);
// Server.default.record(duration: 4, bus: ~o2.index, numChannels:1);
// Server.default.record(duration: 4, bus: ~o3.index, numChannels:1);
// Server.default.record(duration: 4, bus: ~o4.index, numChannels:1);
// Server.default.record(duration: 4, bus: ~o5.index, numChannels:1);
)


